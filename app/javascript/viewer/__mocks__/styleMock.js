// Empty ESM stub for CSS imports under jest (see jest.config.mjs
// moduleNameMapper). CSS carries no behavior the JS unit tests exercise, and
// ts-jest cannot transform a stylesheet.
export default {};
